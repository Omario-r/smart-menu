Что бы компонент не выводил пустую плашку при не заданном значении, ему необходимо на вход подавть пустой массив.
Вариант в модели:
```
city_ids: {
  type: DataTypes.ARRAY(DataTypes.INTEGER),
  defaultValue: [],
},
```